# Legacy location

Manual test scripts that used to live here are archived at [`archive/web_tests/`](../../archive/web_tests/).

The maintained test suite is under [`tests/`](../../tests/) at the repository root.
